<div id="chamado-page">
  <a href="/chamados">Voltar</a>
  <div class="container"><h1>{{chamado.title}}</h1>
    <p>{{chamado.descricao}}</p>
    <div class="actions">
      <a href="/chamado/edit/{{chamado.id}}">Editar</a>
      <form action="/chamado/remove/{{chamado.id}}" method="post">
        <input type="submit" value="Remover" />
      </form>
    </div>
  </div>
</div>