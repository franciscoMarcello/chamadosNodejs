<h1>Edite seu chamado:</h1>
<form action="/chamado/update" method="post" class="form">
  <input type="hidden" name="id" value="{{chamado.id}}" />
  <div class="form-control">
    <label for="title">Título</label>
    <input
      type="text"
      name="title"
      placeholder="Digite o titulo do chamado"
      value="{{chamado.title}}"
    />
  </div>
  <div class="form-control">
    <label for="descricao">Descrição</label>
    <textarea
      type="text"
      name="descricao"
      placeholder="Digite o que esta ocorrendo "
    >{{chamado.descricao}}</textarea>
  </div>
  <input type="submit" value="editar" />
</form>